var assert = chai.assert;
var expect = chai.expect;

describe('countDownTimer', function () {
    describe('#start', function () {
        it('should end within timeot (TODO)', function (done) {
            // TODO: define timer tests
            done(); 
            //var cdt = new CountDownTimer(10);
        });
    });
});
